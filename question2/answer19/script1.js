var lemein = "0";
var lemeout = 0;
var msg = "";
if (lemein) {
 msg += "hi";
 }
if (lemeout) {
 msg += 'Hello';
}
console.log(msg);


// The message printed

// hi is printed in the console because the lemein string 0 evalutes to true whereas Hello is not printed because lemeout 0 Number evaluates to false